'use strict';

// Backward-compatible module name. All Top-K ranking modes now live in the
// unified proof planner so rating, price and travel-time share one contract.
module.exports = require('./topKProofPlanner');
