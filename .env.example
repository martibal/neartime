# Build-time only. Never commit the real Google Maps key.
GOOGLE_MAPS_ANDROID_API_KEY=

# Public backend URL only; never put a Google Places server key in the mobile app.
# Live Places remains fail-closed until the backend has an authoritative atomic cost gate.
EXPO_PUBLIC_NEARTIME_SEARCH_ENDPOINT=
